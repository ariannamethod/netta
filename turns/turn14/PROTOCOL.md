# Turn 14: faster withdrawal of jointly selected prefix memory

2026-09-19, Sol. Frozen before implementation and before worlds 144..151.
Astra's turn13 joint-selection result and Sol's turn6 fixed withdrawal law are
incoming evidence. Their original batches, outcomes and limits remain sealed.

## Question and one composition

Can the stronger 528-byte joint-prefix memory withdraw influence more quickly
after a target law changes while retaining its received benefit on an unchanged
life? Compose two mechanisms without changing either learned source memory or
its local candidate quote:

- `slow`: turn13's present one-way source-to-cold outer HMM, hazard 2^-16;
- `fast`: the already specified turn6 outer law, hazard 2^-10.

The value 2^-10 is not selected on this batch. It is the single rate fixed and
independently verified in turn6, where it matched the local revision timescale,
improved all eight changed tails and retained 90.30% of unchanged gain. This
turn asks whether that fixed authority law composes with turn13's different,
stronger memory. There is no rate sweep or second candidate.

For pretruth cold price C, candidate price S and active source/cold log-odds z,
charge

    L(y) = log2((P_cold * 2^C(y)) + (P_source * 2^S(y)))

before observing y. Only after the charge update the posterior and apply the
stated source-to-cold hazard. Cold is absorbing. Both modes begin with equal
source/cold mass after the same prospective shadow threshold 32; admission
therefore affects the following byte. Before admission L=C. Candidate, shadow,
admission byte, archive, matches, recipient state and cold learner must be
identical between modes. Complete NEW and incomplete contexts retain turn13's
exact behavior.

The absorbing cold path bounds cumulative loss against C by one bit after each
admission. The fast transition leaves at least 2^-10 cold mass after an active
update, bounding interval drawdown by ten bits (plus 1e-7 numerical tolerance).
The controller is two scalar masses and adds no portable archive bytes.

## Everything else stays fixed

Keep turn13's raw-byte generator, four 16-KiB source lives, three 16-KiB target
regimes, HEAD256/P0 frontend, source BPE, proper-prefix pool, all-occurrence
uint16 counts, joint marginal source selection, exact 528-byte cap, longest
stored match, seven candidate arms, KT repeat redistribution, protected NEW,
local recipient and prospective admission rule. Rebuild the memory from fresh
source lives; do not copy a selected record from worlds 128..135.

The seven arms remain `episode`, `isolated`, `frequency`, `reverse`,
`permuted`, `flat`, `row`. Replay both slow and fast authority over every arm,
using the same candidate/cold prices. Selection comparisons use fast authority
for candidate and controls alike. Slow is the exact turn13 law on the new batch.

Fresh worlds are 144..151 under namespace `netta-joint-fast-withdraw-v1`.
The switched target shares the first 8192 raw bytes with recombined and changes
the command law only afterward. Generator labels and change points are never
given to the learner. No redraw, replacement world or threshold edit follows.

## One material gate

All conditions below are fixed now and form one gate.

First, the fast `episode` must satisfy turn13's same seventeen conditions:

1. Mean recombined gain at 4096 is at least 0.005 bit/raw-byte and positive in
   at least six of eight worlds.
2. Mean full recombined gain is at least 0.005 bit/raw-byte and positive in all
   eight worlds.
3. Mean whole-switched `episode-row` difference is nonnegative and positive in
   at least five of eight worlds.
4. Mean changed-tail `episode-row` difference is at least -1 bit.
5. Against each of `frequency`, `row`, `reverse`, `permuted`, and `flat`, the
   mean 4096-byte advantage is greater than 1 bit and positive in at least five
   of eight worlds.

Second, the fixed fast authority must retain and revise the same episode:

6. Its mean 4096-byte and full recombined gains each retain at least 90% of
   slow's corresponding positive mean. Ninety percent is the pre-existing
   turn6 observed retention boundary rounded downward, not a result of this
   batch.
7. Its mean whole-switched episode gain retains at least 90% of slow's positive
   mean.
8. On the changed final 8192 bytes, fast improves episode gain over slow by
   more than 1 bit in mean, improves at least five paired worlds, and raises the
   worst tail across worlds by more than 1 bit.

Finally, all source archives and per-byte candidate prices must be shared;
shadow and admission must match exactly across modes. Both modes keep complete
prefix gain above -1-1e-7; fast interval drawdown is at most 10+1e-7 and slow
drawdown at most 16+1e-7. All quoted distributions are positive and normalized;
protected NEW remains exactly P0. An independent reader must rebuild turn13's
source memories and candidate forecasts, then replay both outer laws as
probability masses and agree within 1e-7 bit.

Report all horizons, every recombined and changed-tail pair, all controls,
candidate versus received prices, activation identities, any negative cases,
and at least one raw byte where fast helps and one where it harms. Preserve a
FAIL with the same detail as a PASS. A PASS establishes only this bounded
composition on fresh exact-role synthetic lives.

## Scope and hand

This step does not establish semantic or functional similarity, independent
source discovery, an accumulated fifty-life traveller, natural language, or a
right to change live Netta, her mouth or mycelium. The archive still stores
explicit exact role strings; faster outer withdrawal does not alter that fact.

No commit, push, merge or live integration belongs to this turn. After the
frozen batch and proportional independent check, return raw help, raw harm and
the complete verdict to Don. The trio rotation is Sol -> Don -> Astra -> Sol.
