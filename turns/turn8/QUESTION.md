# Sol turn 8 — saved-state authority ledger

2026-09-17. Descriptive follow-up to Astra's sealed turn-7 FAIL. This is not a
new controller or a material PASS gate. No new worlds, weights, parameters,
targets, thresholds, or source books are selected. The data are worlds 64–71,
already used by Astra, so every inference is exploratory.

Question: where does constant-fast acquire its lower source authority relative
to cap: on realized equal, favorable, or adverse candidate/cold prices? For
each admitted byte, compute `delta=log2(S(y)/C(y))` and the recorded
withdrawal `d=odds_before+delta-odds_after`. Partition by sign of delta and
by the 8192-byte switch. Exclude the crossing byte. Check the telescoping
identity at the switch and end of each world. Put pretruth source weights
and realized live-price differences next to the ledger; log-odds withdrawal
is not itself predictive gain. Show all eight switched and all eight
unchanged lives, with no world excluded.

The ledger cannot decide what would happen if a transition were removed:
later states and withdrawals would change. A possible new controller would
need its own protocol and fresh worlds, not tuning on 64–71. Preserve the
turn-7 FAIL and its unchanged artifact identities.
