# Independent reader — five-bit commitment ceiling

2026-09-21. Incoming turn18 reproduction is preserved in `audit/`.
This reader derives from that sealed independent reader. The new experiment
writer and authority C sources were neither read nor imported. Only the frozen
`turns/turn13/verify.py` reader and standard-library modules are imported.

- Protocol SHA256: `e00991a388a12b6ee3dbc803f1de613752c141944ed5ee30fe46ead7de53f71b`.
- Reader SHA256: `0400e718b987a0c33b529e1ba854afb0655b9b3f5038e8e9f2acb0caa8a928fc`.
- CLI check: `PYTHONDONTWRITEBYTECODE=1 python3 turns/turn19/verify.py --help`, exit0.
- No fresh world, new target prediction or parameter search run by this hand.

## Independent arithmetic and retained contracts

The old reader rebuilds source grammar, joint prefix choice, seven exact
archives, relation histories, matches and candidate prices. The new reader
also recounts the episode matched length from source-derived literal content.
New paths explicitly resolve repository root above `turns/`; frozen files
are imported unchanged and checked by the new freeze/manifest receipts.

Five normalized Decimal50 source/cold mass states replay the same candidate
and P0 tape. Ceiling first performs the ordinary witness Bayes/hazard update,
then applies `source=min(source,Decimal(32)/Decimal(33))` and `cold=1-source`.
The clip indicator compares the uncapped updated mass with32/33. This avoids
using the new C log-odds implementation as its own oracle.

The clock update, matched predicate and admission law are inherited exactly.
Before/after evidence values of ceiling and witness are compared with strict
equality on every row, in addition to own state reconstruction. Current
quotes precede each mass update. Every inactive/equal-price and protected NEW
quote is checked for exact equality with P0, without numeric tolerance.

All recorded quotes, hazards, odds, clocks and clip flags must match the
independent replay within1e-7bits (flags exact). All32 complete lives are
checked:3,670,016 candidate forecasts and2,621,440 authority forecasts.
Mode statistics include clip count, maximum odds (starting at0), odds at
index8192, early/full/tail gains, first admission and clock shares. The reader
recomputes all21 material conditions, including actual clipping in all eight
recombined lives and the five-bit cap throughout. Prefix gain stays>=−1;
interval bounds are log2(33) for ceiling,10 for fast and16 for other modes.

Changed-life help and harm are separately rebuilt as the maximum/minimum
ceiling-minus-witness price among every active byte after index8192. The
saved records include all five prices, before/after odds, actual truth/rank,
match, evidence and clipping. An early or whole-life cost remains visible
beside a changed-tail improvement.

## Scope

HEAD256 bindings and sparse P0 remain supplied trace inputs. Full local
segmentation and all256 cold probabilities are not regenerated. The inherited
candidate reader reconstructs role probabilities and checks frozen C full-vector
normalization/NEW receipts. The new mixture and clipping are independently
computed as positive normalized masses.

This verifies one fixed five-bit ceiling and its stated gate. A result does
not decide every possible rate or level-control rule. Gate failure remains
separate from a reader mismatch. No sealed predecessor file is changed;
output requires an unused `--output` path, default `turns/turn19/VERIFY.json`.
