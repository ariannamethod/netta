# Turn24 ORACLE-LATENCY — Don's acceptance record and verdict

domain: arianna-method.netta.turn24-oracle-latency-report/v1
2026-09-22. Builder: Opus subagent under the preregistered PROTOCOL.md,
with one course-correction exchange before code (the builder's seam
derivation was corrected against the generator law; the builder then
corrected the correction's scope — on command tapes the switched tail IS
unrelated's tail, turn13/experiment.py:91, while on emitted bytes it is
not; both facts are recorded). Acceptance, reruns and this record: Don's
hand.

## Verdict

**gate_pass = false via W3 alone — and W3's failure is the turn's second
discovery.** Recounted by my hand from the artifacts; the reader computes
identical W verdicts (writer marks W6 unobserved before the reader runs,
the honest turn21 convention): W1 PASS, W2 PASS (d* = 32; rate-only
d* = 128), W3 FAIL, W4 PASS, W5 PASS, W6 PASS in VERIFY.json.
7,340,032 arm-forecasts recomputed, max numeric error 1.0346e-10;
pristine rerun by my hand, zero differing fields; integrity probe refused
BY NAME with the full path, rc=1 — the turn23 nameless-refusal regression
is corrected as the protocol ordered.

## The two discoveries

1. **The game is winnable, and the bar-set is sound (W1).** O-level-0
   clears all eight bars: law-tail 0.0000 (it is fast in the tail by
   construction), law-whole +10.5597, moved +10.9249 at 8/8, retention
   1.0/1.0. Four mechanism FAILs were misses at a reachable target, not
   proof of an impossible one.

2. **Turn18 is overturned by half (W3).** O-rate-0 — perfect timing,
   rate-only response, KEEPING the ~6-bit odds stock — beats the teleport
   envelope on law tails (+0.2742 vs 0.0000) and clears every bar at
   every d up to 128. The clamp costs −0.27..−1.52 bits mean and helps in
   at most 3/8 worlds; the slow-fast odds gap (6.005–6.706 bits on all 32
   switches) never inverts. Standing correction to my turn18 conclusion:
   **the debt is a level only when the rate answers late; answered on
   time, the retained stock pays for itself even after a law change.** Do
   not throw away commitment — just stop feeding it.

## The number the arc was missing

The latency budget for a rate-only causal mechanism is **128 bytes**. The
witness clock's measured detection median is 38.5 bytes. The gap between
what causal detection already achieves and what the game requires is a
factor of ~3.3 — so the four FAILed mechanisms did not fail at detection.
They failed at the POLICY AFTER detection: the witness clock un-latches
(guilt decays, w rises back above −1, the clock returns to slow inside a
foreign law), and on this fresh batch witness fails b1 at −4.0588 while
O-rate-32 — same latency class, but permanent — passes everything.

## Acceptance performed (all rc direct)

- Recount: 6 W-families both hands, identical verdicts; quantities and
  the d-curve re-read from RESULT.json.
- FREEZE: 36 pins; two files at their amended SHA-256 per
  FREEZE_AMENDMENT.json, all others at frozen values, unlawful drift
  NONE.
- Oracle law read line by line: the hidden label's entire use is one
  guarded test (oracle.c:103-114); single-switch, pre-switch identity to
  slow and post-clamp identity to the fast envelope are runtime asserts
  on every byte, not claims.
- Pristine reader rerun: rc=0, zero differing fields.
- Red probe: one digit in results/world244/moved_mid.tsv.gz → refusal by
  name with full path, rc=1.
- Equivalence of reference arms accepted on the builder's bitwise probes
  against FREEZE-authentic turn18/turn21 binaries (0 mismatches over 21-22
  columns × 3 lives × 16384 rows; episode binary cmp equal); selfnorm and
  split rest on pinned sources plus the Decimal reader's 1e-10 agreement,
  stated as such without overclaim.

## Ruling on FREEZE_AMENDMENT.json — accepted, and made law

Both frozen scripts crashed identically on a constructional fact (labels
stage read *.commands.bin for moved_mid, which is built by surface
bijection and has none) before any RESULT existed. The repair is one line
in each script; FREEZE.json itself untouched; an amendment-aware
check_freeze accepts a file at exactly its frozen or amended sha and was
shown to refuse a third state by name; all binaries byte-identical to the
pre-data freeze; worlds never regenerated. Standing law from this ruling:
a post-freeze repair is lawful ONLY when (a) the defect is a crash, not a
result; (b) no RESULT exists yet; (c) the amendment is a separate signed
record with reason, law-neutrality statement and exact shas; (d) the
freeze checker enforces frozen-or-amended-and-nothing-else. Anything
outside those four walls remains what it always was.

Also preserved: the write-gate blocked the builder's drafted "inverts by
byte 8320" claim in RAW.md; the data refutes it (the gap never inverts on
any of 32 switches). The walls catch narrative, not just code.

## One bounded next question (a proposal, not this turn)

THE LATCH. One-line causal law: the witness clock as built (turn18), with
one change — the first time w <= −1, the hazard switches to 2^-10 and
LATCHES; no return to slow on that life (or a preregistered re-arm law
with a declared, fixed threshold — one of the two, chosen before data,
not both). Turn24 supplies its specification: detection at ~38.5 bytes
against a 128-byte budget, no clamp (the stock stays), rate-only
response. If the latch clears the bars, the arc has its first causal
PASS; if it fails, the failure will name the first mechanism defeated by
something other than timing or level.

— Don (Fable, neo). Rotation per Oleg's word: Sol → Don → Astra. Worlds
240..247 sealed.
