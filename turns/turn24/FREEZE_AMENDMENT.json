{
  "created_utc": "2026-09-21T23:01:46Z",
  "data_untouched": "Worlds 240..247 were generated once under the pre-data pin and are not regenerated. No result had been computed when the repair was made: RESULT.json did not exist and evaluate had not run.",
  "enforcement": "check_freeze accepts an amended file at exactly two shas, the frozen one and the amended one recorded here, and nothing else.",
  "files": {
    "turns/turn24/experiment.py": {
      "amended_sha256": "d8b382abd1637bdbbb3a0404f538b8ebdfd754f76e4b722e966f37c73ef74417",
      "diff": "--- /tmp/claude-501/-Users-ataeff/e5d37854-03d6-4638-ab3a-4f4e6a33a626/scratchpad/experiment.prefreeze.py\t2026-09-22 01:59:28\n+++ experiment.py\t2026-09-22 02:01:27\n@@ -100,12 +100,26 @@\n \n \n def check_freeze():\n+    \"\"\"The pre-data pin, plus any recorded amendment.\n+\n+    FREEZE.json is never rewritten. A file repaired after the batch existed is\n+    listed in FREEZE_AMENDMENT.json with both shas, and is accepted at exactly\n+    those two states and no third one.\n+    \"\"\"\n     frozen = json.loads((HERE/'FREEZE.json').read_text())\n     assert frozen['namespace'] == NAMESPACE and frozen['worlds'] == list(WORLDS)\n     assert frozen['arms'] == list(ARMS) and frozen['delays'] == list(DELAYS)\n     assert frozen['seam'] == MOVE and frozen['seam_regime'] == SEAM_REGIME\n+    amendment = HERE/'FREEZE_AMENDMENT.json'\n+    amended = {}\n+    if amendment.exists():\n+        note = json.loads(amendment.read_text())\n+        for name, pair in note['files'].items():\n+            assert pair['frozen_sha256'] == frozen['files'][name], name\n+            amended[name] = pair['amended_sha256']\n     for name, wanted in frozen['files'].items():\n-        assert digest(ROOT/name) == wanted, name\n+        got = digest(ROOT/name)\n+        assert got == wanted or got == amended.get(name), name\n \n \n def generate():\n@@ -143,7 +157,10 @@\n     for w in WORLDS:\n         folder = HERE/'data'/f'world{w}'\n         raw = {r: (folder/(r+'.bin')).read_bytes() for r in REGIMES}\n-        cmd = {r: (folder/(r+'.commands.bin')).read_bytes() for r in REGIMES}\n+        # moved_mid carries no command tape: turn22's generator makes it by\n+        # applying a surface bijection to recombined.bin, not by emission.\n+        cmd = {r: (folder/(r+'.commands.bin')).read_bytes()\n+               for r in ('recombined', SEAM_REGIME, 'unrelated')}\n         assert all(len(b) == N for b in raw.values()), w\n         assert all(len(b) == N for b in cmd.values()), w\n         prefix = raw[SEAM_REGIME][:MOVE] == raw['recombined'][:MOVE]\n",
      "frozen_sha256": "6fd697a2b9dab3de8c981cdbc9b27b7861e5270e4cc233948650799b6fb90e2b"
    },
    "turns/turn24/verify.py": {
      "amended_sha256": "b196a2dfe05077c3e6d83a3cbd28dcad7b60a5a53bc68187741e59dd9e1a4ade",
      "diff": "--- /tmp/claude-501/-Users-ataeff/e5d37854-03d6-4638-ab3a-4f4e6a33a626/scratchpad/verify.prefreeze.py\t2026-09-22 01:59:28\n+++ verify.py\t2026-09-22 02:00:51\n@@ -306,7 +306,9 @@\n     \"\"\"Re-derive the hidden label from the generator's own data, independently.\"\"\"\n     folder = HERE/'data'/f'world{world}'\n     raw = {r: (folder/(r+'.bin')).read_bytes() for r in REGIMES}\n-    cmd = {r: (folder/(r+'.commands.bin')).read_bytes() for r in REGIMES}\n+    # moved_mid carries no command tape; it is a surface bijection of recombined.\n+    cmd = {r: (folder/(r+'.commands.bin')).read_bytes()\n+           for r in ('recombined', SEAM_REGIME, 'unrelated')}\n     need(all(len(b) == N for b in raw.values()), f'world {world} target length')\n     need(raw[SEAM_REGIME][:MOVE] == raw['recombined'][:MOVE],\n          f'world {world}: the switched prefix is not the recombined prefix')\n",
      "frozen_sha256": "6264d47d72278969e59c06fbc7d39f35c7f77f2672d1e238e79e303474f9eee8"
    }
  },
  "law_neutrality": "The diff touches three hunks: the same one-line command-tape repair in each script, and an amendment-aware check_freeze. No arm, bar, W-test, oracle law, clamp rule, reader law, tolerance or world is changed. The eight bars, the six W tests, oracle.c and every binary are byte-identical to the pre-data freeze.",
  "reason": "labels() in experiment.py and derive_labels() in verify.py read *.commands.bin for all four regimes, but moved_mid has no command tape: turn22's generate_world builds it by applying a surface bijection to recombined.bin rather than by emission. Both scripts raised FileNotFoundError on data/world240/moved_mid.commands.bin. The crash was found after worlds 240..247 existed, so the repair could not be folded into the pre-data freeze.",
  "status": "POST-DATA REPAIR. FREEZE.json is unchanged and still records the pre-data pin."
}
