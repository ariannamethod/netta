# MYCELIUMLOG

The arc's own log, in the arc's own folder. The heart's log stays
NETTALOG2.md; nothing here reaches into it.

## Body 1 — the graft (2026-08-20)

The constitution went first and the audit reached it while the concrete
was still wet: the second hand landed six findings on the roadmap before
any code existed, and the amendment commit folded every one in — the
attestation law renamed to what a stateless reader can prove, the
prototype's dead edges left buried, the ledger made event-sourced, the
dedup key moved from content to witness, the byte law declared, the
gate's refusal battery separated from the mechanism's ablation. One
refinement was named after the amendment and before code: the pinned
biography digest covers the attestation PREFIX — the biography's bytes
through the matched s line — because a whole-file digest would let the
same s event be eaten twice once the life lived further and the file's
bytes moved.

`mycelium.cpp` (C++17, stdlib only, strict build clean) ports the
resonance mechanics of microkarpathy's `mycelium.py` under the byte law
and adds the ledger and the gate; `ledger_check.c` is the independent
reader, plain C, no shared code. The battery ran in numbered rooms with
two lives raised on different worlds (Dracula and Frankenstein prefixes),
each speaking and signing its own stream.

The first battery room returned 12 pass, 1 fail — and the failure is the
log's favourite line: the preregistered ablation REFUSED its own organ.
At 3,600 lived bytes both lives spoke with the uni hand, and uni-order
byte soup carries no per-life lexical identity: metric A (top-1 clause
returns to its origin's life, origin excluded) sat at chance, 4/14 real
against 5/14 for the hash-vector control. The sealed prediction was
allowed to fail in public, which is the only reason it means anything.
One hypothesis — the input, not the organ — and the lives were raised to
27,600 lived bytes, where both mouths elected the bi hand and the speech
became word-shaped.

Second room, same sealed metric, untouched: 13 pass, 0 fail. Ablation
real A=60/98 (0.612) against control 44/98 (0.449), real B=0.2581
against 0.1938 — the resonance organ finds the life a clause came from
better than its absence does. The corpse of the standing prompt drew on
2/2 lives with full lineage. All four gate refusals (unsigned shuffle,
one flipped byte, re-ingest of an eaten pair, the same pair re-offered
through a grown biography) left the ledger byte-identical. Both hands —
writer replay and `ledger_check` — refused all three tamper classes
(blob content, ledger byte, missing blob) in the same verdict class, and
agreed on the honest chain to the last hex digit. A clean-room repeat of
the full sequence reproduced grave, ledger and stdout byte for byte.

Named limits, standing for the audit: the gate attests the pair, not
authorship — the state-witness is a future organ; a ledger prefix cut at
a record boundary is self-consistent and needs an external witness; the
ablation's verdict depends on the lives' maturity, and the uni-hand red
run is the proof that the metric can say no.

### The twin probe (same day, after the graft)

The dedup-by-witness law was run, not just claimed. Two lives whose
biographies diverge before the matching s line, speaking byte-identical
streams (cmp-proven), land as two G events over one interned blob —
ledger_check green, field names two lives and two attested pairs over one
body. The road there surfaced the edge: perfect clones — identical world
and seed, histories identical through the matching s line — refuse the
second offering as already eaten, because prefix, ordinal and s line are
all byte-identical. Indistinguishable evidence is one event; the position
stands in the handoff for the second hand's verdict.
