# MYCELIUM ADAPTATION — TURN 11 CONTRACT (before any code)

domain: arianna-method.netta.mycelium.adaptation-contract/v1

Status: counter-audited and repaired by Sol; body A1 now exists in a
first-hand implementation and remains powerless pending Don's counter-audit.
No diet, circulation, or reverse channel is authorized by this document.

## 1. Ground

The pause of 2026-08-24 named three conditions for reopening this arc.
All three are closed: the ZERO mouth question dissolved together with
the ZERO line itself; a new preregistered court sat to the end and
returned its literal verdict — `CONFIRMATORY PASS: microscopic
relation replicated in selected class`
(`COURT4_DRAW2_C8_VERDICT_RECORD.tsv`,
`b528c3e2efe1a2dd38b892e18f67e2db98338b61e617c1b581f119686fc76b1e`);
and the explicit word arrived on 2026-09-13. Bodies 1–5 survived byte for
byte at merge `535198b`, although `main` later removed the whole directory
at `2859e5e`; they are restored here from that exact tree before adaptation
code is added. Turn 10 contributed two preregistered contracts to
`MYCELIUM.md`, not an implementation. `main` carries the court-4 line and
its published source capsule (`court4/`, commit e02c645).

## 2. What survives unchanged

The constitution, bodies 1–5, and their ladder: proposer → school →
parliament → note-weights; contract before code; two hands on everything; ballot =
the citizen's full identity history, cherry-pick impossible by
construction; the no-authority rows; one body — one organ of power.
The standing obligation stays load-bearing: a citizen is an opaque
`unit_id` plus a versioned recognizer/receipt, never a string shape.
The three substrate courts' verdicts stand: byte law holds, the cone
is a named limit, u8b remains the cheap escape.

## 3. What died with the substrate — named, not repaired

The s/w-line grammar of the ZERO biography as the mycelium's future input.
It remains historical grammar for replay of bodies 1–5 and is not an
adaptation mouth. diet-v1, whose admission constants quoted printability
rather than eloquence and were falsified by the long sitting. Turn 10's
diet-v1 and circulation contracts stay as historical text: no turn-10 code
was built, so there is no construction to repair or resurrect.
The circulation mechanism's design language — spending all lawful
weights at once, both outcomes sealed in one atomic row, two hands
recomputing everything — is kept as language for a future contract;
the construction that spent weights against the dead diet is not.

## 4. The new substrate — facts on disk

The court-4 organism already has candidate citizens, and they are earned,
not declared: an identity is the organism/arm plus `RelationKey`
(court4/transfer4.c:922) — a pair with its context pairs and epochs,
promoted "to an exact RelationKey after 32 prospective bits; remaps never
inherit" (court4/transfer4.c:6). `RelationKey` alone is insufficient:
the same key may occur in the learned relation arm, the privileged oracle,
and null controls. Only `arm=relation` belongs to the candidate population;
oracle and null rows remain controls and can acquire no mycelium office.

Advice-right is paid in prospective bits and sealed as EARN and REVOKE
transitions; the one-shot run's books and evidence are artifacts pinned by
the C8 record. `relation_events` contains threshold crossings, not every
receipt delta. It can replay the admitted/revoked state but cannot by itself
recompute a final floating-point balance. That balance is witnessed from
the pinned final `relations` book and checked against counts, thresholds,
context-zero exclusion, and the last transition. Re-executing the Court-4
builder or verifier is explicitly outside A1. C8 proved one microscopic
relation class transfers: a relation earned on SOURCE_A replicated on an
unseen base in the drawn class ff. It did not prove scale and does not make
all 162 relation rows citizens of the mycelium.

## 5. Diet v2 — principle frozen now, thresholds only by court

A candidate may enter the future diet court only with a live earned balance
in the learned relation arm's sealed final book: at least one EARN for the
same arm-plus-key, no later REVOKE, `state=1`, and lawful nonzero `L`. A1
records that fact but does not perform admission. Every
admission constant enters from a committed record, with the record
named; no constant is ever quoted from a narrative sentence — that
is the exact lesson of diet-v1's death. A new preregistered court
judges diet-v2 on this substrate before any circulation body exists;
the pause's spirit — no spending before the diet is judged — is
carried forward as law.

## 6. Body A1 — the witness, zero authority

The portable capsule under `court4_biography/` contains only the 38,220
bytes that the adapter needs: an ordered manifest of the six procedural
stage identities plus byte-exact copies of `relation_events` and
`relations`. The reports, binaries, large evidence, and other Court-4
artifacts remain in the ignored closure archive. The manifest pins the
published source commit, literal C8 verdict, every stage's bytes/SHA-256,
both book identities, and the census derived below.

`adaptation_witness.cpp` is an adjacent C++17 OOP body, not a branch inside
the old ZERO mouth. It reads only regular files, checks the frozen Court-4
pins, parses both books, identifies citizens by arm plus all 13 canonical
`RelationKey` fields, and independently derives the transition and
candidate/control census. It writes only `.mycelium.court4-witness` and its
content-addressed `.d/` blob. The blob carries the exact three input
sections, with lengths and SHA-256 identities; one two-row FNV-sealed ledger
points to it. A repeat is byte-idempotent. A partial or disagreeing prior
output refuses rather than repairs.

`adaptation_check.c` is the second implementation of the reader path. It
shares no parser or hash code with the writer, opens the content-addressed
blob, recomputes its section hashes, reparses every transition and relation,
and rejects a false census even after a canonical reseal. Its existence does
not replace Don's counter-audit: two programs in one author's hand are two
implementations, not two people.

Zero authority is structural and tested: A1 has no link to `mycelium.cpp`,
does not open the grave or any bodies 1–5 chain, cannot ingest, propose,
enrol, vote, mint, rank, speak, alter Court 4, or circulate a weight. The
five live learned-arm relations it witnesses are evidence for the future
diet court, not members admitted by A1.

## 7. Turn order

- turn 11 — Don's adaptation contract; Sol's counter-audit, historical
  restoration, compact biography capsule, and first-hand body A1;
- next hand — Don counter-audits the repaired contract, both A1 programs,
  the exact capsule derivation, and the red gates; repairs precede any new
  organ;
- next body — the diet-v2 court, preregistered before its implementation;
- circulation only after that court HOLDS.

## 8. Boundaries

The `court4/` source capsule, the sealed records, and every frozen artifact
remain untouched. The two compact book copies are identified byte for byte;
they are not rewritten evidence. Bodies 1–5 are restored byte-identically
from `535198b`; their old chains remain replayable under their old laws.
The historical diet-v1/body-6 text is superseded only for future work, not
deleted from history. Two hands on every body, as always.

— Don (first contract) and Sol (counter-audit and A1 first hand), 2026-09-13
